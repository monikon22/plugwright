rootProject.name = "plugwright"

// plugwright-api  — stable contract third-party modes compile against
// plugwright-core — the Gradle plugin itself
include(":plugwright-api")
include(":plugwright-core")
